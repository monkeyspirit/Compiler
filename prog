module x (v1: int, v2: string): int 
var i, j, k, h, g: int;
const MAX: int = 2;

	module y (v3: int): real 
	var i: int;
	const MIN: int = 2;
	
	begin y 
	c = f;
	end y

begin x 
a = i * d and i + c ;
g = f / g;
end x
